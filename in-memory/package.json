{
  "name": "in-memory",
  "version": "0.1.0",
  "scripts": {
    "build": "bsb -make-world",
    "build:w": "bsb -make-world -w",
    "format": "bsrefmt --in-place src/*.re src/*/*.re __tests__/*.re",
    "test": "jest",
    "test:w": "jest --watch",
    "clean": "bsb -clean-world"
  },
  "keywords": ["BuckleScript"],
  "author": "Bruno Michel",
  "license": "MIT",
  "devDependencies": {
    "@glennsl/bs-jest": "0.5.1",
    "bs-platform": "^7.3.2"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
